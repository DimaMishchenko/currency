# ForegroundRefresh

Coordinates automatic refresh across active app scenes.

The first active scene starts shared work; the last inactive scene cancels it. Hosts supply refresh capabilities, while domain services own freshness and persistence rules.
