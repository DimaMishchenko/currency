# Conversion

Amount editing, currency selection, and persisted converter input. `Conversion` owns these rules and records; `CurrencySelectionUI` provides the reusable currency chooser and its resources.

Keep formatting and navigation outside the editing rules. Preserve the distinction between a fixed currency and dynamic Local intent, and coordinate persisted edits with other writers.
